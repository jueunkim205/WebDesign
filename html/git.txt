[git 설치하고 연결하기]

git config --global user.email "이메일"
git config --global user.name "이름"
git config --list

--------------------------------------
git remote -v (내 정보 뜸)

--------------------------------------
폴더가 없어도 vscode 메인화면에서 "git 레포지토리 복제" 선택하면 됨

--------------------------------------
초기화시
내 로컬폴더에서 git폴더 삭제,  자격증명관리자에서 git 삭제
했던대로 git 설치 -> 커밋시도 -> 로그인 -> 커밋